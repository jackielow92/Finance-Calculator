# Frontend (VITE_ prefix required for client-side access)
VITE_API_URL=              # Empty for GitHub Pages static mode
VITE_APP_TITLE=Financial Calculators

# Backend (for future use)
PORT=3000
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173
